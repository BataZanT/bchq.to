<h1>Links for <%= Current.user.username %></h1>

<div class="list-group">
  <% @links.each do |li|%>
    <%= button_to li.title, li.url, method: :get,  class: "list-group-item list-group-item-action" %>
  <% end %>
  
</div>